class Solution {
public:
    int count=0;
    int findNumbers(vector<int>& nums) {
        for(int i=0;i<nums.size();i++){
            if(std::to_string(nums[i]).length()%2==0) count++;
        }
        return count;
    }
};
